from distutils.core import setup
setup(name='we',
      version='0.0.1',
      author="Patrick O'Neill",
      author_email="pon2@umbc.edu",
      #py_modules=['we'],
      packages=['we'],
      package_dir={'we':"src"}
      )
